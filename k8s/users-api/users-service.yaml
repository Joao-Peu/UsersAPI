apiVersion: v1
kind: Service
metadata:
  name: users-api-service
  namespace: microservices
  labels:
    app: users-api
spec:
  selector:
    app: users-api
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  type: ClusterIP
  sessionAffinity: None